# Database
DB_HOST=db
DB_PORT=5432
DB_USER=app
DB_PASS=changeme
DB_NAME=workshop

# App
BACKEND_PORT=3000
FRONTEND_PORT=3000

# API keys
# Admin/API key for protected endpoints (set same value to NEXT_PUBLIC_API_KEY for frontend builds if required)
ADMIN_API_KEY=changeme-api-key
# Expose short-lived/public key to frontend builds (only if acceptable)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
NEXT_PUBLIC_API_KEY=changeme-api-key
